export enum ReturnBagStatusTypes {
  initiated = "initiated",
  dispatched = "dispatched",
  confirmed = "confirmed",
  delivered = "delivered",
  processing = "processing",
  in_verification = "in_verification",
  live = "live",
  cancelled = "cancelled",
}
